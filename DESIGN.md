# DESIGN.md — mmj-map

**このプロダクトでは、DESIGN.md が飾りではありません。**
地図スタイルはこの製品の中身そのもので、色を決めることが仕事です。

> **人が書く文書です。** 実際の地図を見た人が書きます。
> 生成された配色を貼り付けると、この文書は意味を失います。

> **この版について（2026-09-17）。**
> 以下の色は **AI が考えた配色ではありません。**`styles/modern-dark.json` に
> 実装されて画面に出ている値を、**人が実物を見て承認したうえで転記したもの**です。
> 承認の経緯は次のとおりです。
>
> - 人が `docs/screenshots/` の 15 枚を見た。指摘は 1 件（ポップアップ・修正済み）
> - 提案（`.claude/proposals/2026-09-16-design-umeda-namba.md`）の配色を実際に適用して
>   同じ画角で撮り比べ、**実装のほうを採ると人が決めた**（下の「採らなかった配色」）
> - 1 枚目をダークにすることは D-016 で決定
>
> **数値はすべて実測です。**コントラスト比は WCAG 2.1 の相対輝度から計算しています。

## 1. 何を決める文書か

| 決める | 決めない |
|---|---|
| 地図の配色・ラベル・レイヤの見え方 | デモサイトの装飾 |
| ダーク / ライトの性格づけ | ドキュメントサイトの見た目 |
| UI 部品（Marker / Popup / Cluster）の既定の見え方 | 利用者側のアプリのデザイン |

## 2. 地図スタイル

### テーマ

| テーマ | 状態 | 性格 |
|---|---|---|
| Modern Dark | **1 枚目に確定**（D-016・2026-09-17） | **既存の地図サービスとの違いが、ひと目で分かること。** 主要なサービスは明るい地図が既定なので、暗いことがそのまま差になる |
| Modern Light | 未着手 | **比較のためには作らない。** テーマを差し替えられる仕組みが機能していることの証明として作る（D-016） |

**テーマは、導入する側が選ぶもの**という前提で作ります（D-016）。
1 枚目を作り込むことより、**2 枚目を足せる形を保つこと**を優先します。

### カラーパレット

**`styles/modern-dark.json` が正本です**（D-002 — スタイルは生成物ではなく作品）。
この表は、その正本を読める形に写したものです。**食い違ったら正本が正。**

| 役割 | 値 | レイヤ |
|---|---|---|
| 背景・陸地 | `#1B1F24` | `background` / `earth` |
| 地表被覆 | `#1E252A` | `landcover` |
| 緑地 | `#1C2A22` | `landuse-green` |
| 市街地 | `#21262C` | `landuse-built` |
| 舗装地 | `#23282E` | `landuse-paved` |
| **水域** | `#14303D` | `water` / `waterway` |
| 建物 | `#262C33` | `buildings` |
| 小径 | `#2A3037` | `roads-path` |
| 細街路 | `#2E343B` | `roads-minor` |
| 二次道路 | `#363D45` | `roads-medium` |
| 主要道 | `#3E464F` | `roads-major` |
| 高速（縁） | `#363E47` | `roads-highway-casing` |
| **高速** | `#4E5863` | `roads-highway` |
| 鉄道 | `#5A6470` | `rail` |
| 境界 | `#5A4C3E` | `boundaries` |

### ラベル

| 役割 | 文字 | 縁取り | 地に対するコントラスト |
|---|---|---|---|
| 市名 | `#D8DCE1` | `#111418` | **12.0 : 1** |
| 駅名 | `#C9D0D8` | `#111418` | **10.6 : 1** |
| 町名 | `#AAB2BC` | `#111418` | **7.7 : 1** |
| 水域名 | `#8FA9B8` | `#111418` | **6.7 : 1** |

**4 段とも WCAG AAA（7 : 1）を満たすか、それに近い**値です。
重要度の順序が、そのままコントラストの順序になっています。
**縁取りは全段共通**（`#111418`）で、地より暗い側に置いています。

### 面の段差

| 隣り合うもの | 明度比 |
|---|---|
| 地 → 建物 | 1.18 : 1 |
| 地 → 水域 | 1.20 : 1 |
| 建物 → 細街路 | 1.12 : 1 |
| 細街路 → 幹線 | 1.74 : 1 |

**輪郭線を使わず、面と明度差だけで読ませています。**
建物密度の高い地区（船場〜心斎橋）で 1px の輪郭を引くと、
z15 では建物ではなく灰色のノイズになるためです。

### 採らなかった配色（2026-09-17）

`.claude/proposals/2026-09-16-design-umeda-namba.md` の 8 段のパレット
（`L0 #12151C` / `L1 #080B11` / `L2 #1B202A` …）は**採りませんでした。**

**実際に適用して、同じ画角（梅田〜中之島 z15）で撮り比べたうえでの判断です。**

- **水がほぼ黒（`#080B11`）で、川と空白の区別がつかなかった。**
  提案自身が「この矩形で確かめるべきは 1 点 — 中之島が島として読めるか」と
  書いていた、まさにその判定で落ちました
- **建物と地の差が小さく、のっぺりした**
- **提案に緑の段が無いため、緑地が消えた。**8 段しか定義されていないのに
  実装は 22 レイヤあり、当てはめる時点で寄せ先を決めるしかありませんでした

あの提案は**地図が 1 枚も描けていない時点で書かれた仮説**で、
本文に「実物を見た瞬間に覆りえる」と書かれています。**覆りました。**

### 外界が色を決めているもの（配色ルールの適用外）

**ここは作るものではなく、写すものです。**

- **鉄道の路線色** — 事業者が定めた色をそのまま使います。
  パレットに合わせて振り直すと、**利用者は路線を読み違えます。**
- 信号・道路標識の色
- 第三者のブランドカラー

→ パレットの調和より、**現実との一致を優先します。**

**まだ写していません。** いまの `rail`（`#5A6470`）は事業者の色ではなく、
**路線を区別していない**状態です。写すときの規則は決めてあります。

- **彩度を落とさない。** 馴染ませるために彩度を下げた時点で、写した意味が消えます
- **線幅は全路線で同じ。** 幅で差をつけると、事業者の序列を勝手に作ることになります
- **地上と地下は、色でも破線でもなく重なり順で表す。** 地上が上。
  地下鉄を破線にすると、大阪の中心部では画面が点線だらけになります

## 3. 日本語の地図に固有の条件

この 3 つが、汎用スタイルをそのまま当てられない理由です。

- **地名の密度が高い。** 同じ縮尺でも、ラベルの衝突が起きやすい。
- **漢字・かな・英数が混ざる。** 文字ごとに字面の重さが違い、
  同じフォントサイズでも見え方が揃わない。
- **鉄道網が濃く、事業者ごとに色がある。**（上記）

### 書体は、まだ決められません

Protomaps が配るフォントは **Latin のみで、CJK のスタックがありません**。
いまは `localIdeographFontFamily` で閲覧側のフォントに逃がしているため、
**字形が閲覧環境ごとに変わります。**組版を作品として作り込めません。

ここは `.claude/decisions.md` の未決です。**この文書で先に決めません。**

## 4. UI 部品の既定の見え方

**既定であって、固定ではありません。** どれも属性で差し替えられます。

| 部品 | 既定 | 出どころ |
|---|---|---|
| Marker | `#3FB1CE` | **MapLibre が元から使っている色。**新しく作っていない |
| Popup（地） | `#1B1F24` | 地図の背景と同じ |
| Popup（文字） | `#D8DCE1` | 市名ラベルと同じ |
| Popup（縁） | `#2E343B` | 細街路と同じ |
| Cluster（丸） | `#3FB1CE` | Marker と同じ |
| Cluster（件数の文字・丸の縁） | `#111418` | ラベルの縁取りと同じ |
| 自前 POI（点） | `#3FB1CE` | Marker と同じ |
| 自前 POI（文字 / 縁） | `#D8DCE1` / `#111418` | ラベルと同じ |

**部品は新しい色を持ち込みません。**
この表の値がスタイルに実在することは、テストで機械的に縛っています
（`packages/elements/test/attrs.test.js` の `POPUP_COLORS`）。

### 件数は、色ではなく大きさで表します

クラスタの件数を色の段階（少ない=青 / 多い=赤）で表しません（D-011）。
**凡例のない色の段階は、読む人ごとに違う意味に読まれます。**
大きさなら、後から配色を変えても壊れません。

### ポップアップに閉じるボタンを出しません

MapLibre の既定（`closeButton: true`）は、× を箱の右上へ絶対配置します。
**短い文字（「大阪城」など）だと箱が狭く、× が文字に重なって潰れます。**
実物で発生しました。地図か目印を押せば閉じるので、**閉じる手段は残っています。**

## 5. レスポンシブ

**記入待ち。** 地図は「画面が小さいと縮む」ではなく
「画面が小さいと**見せる情報が変わる**」で設計します。

**まだ実物で確かめていません。**撮っているのは 1264px 幅だけです。

## 6. 必ず画面に出るもの

- `© OpenStreetMap contributors` — **どのテーマでも読める配色にする。**
  背景に溶かして見えなくすることは、帰属表示をしていないのと同じです。
  現状は MapLibre の既定（`attributionControl`）のままで、
  **撮った 15 枚すべてに読める状態で写っていることを確認済み**です。
