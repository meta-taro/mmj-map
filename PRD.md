# PRD — MMJ

> **正式名称は MMJ**（D-018・2026-09-21）。リポジトリ名 `modern-map-japan` は動かしません。
> **対象は日本に限りません。**道具は地球全体で動き、地域ごとに持つのはスタイルです。

**この文書は原案の要約ではありません。** 原案は
[`docs/origin/proposal-2026-09-04-ja.md`](docs/origin/proposal-2026-09-04-ja.md) に
手を入れずに置いてあります。ここに書くのは **原案から何を動かしたか** です。
動かしていない部分は原案を読んでください。

---

## 0. Product Gate — 最初の 5 問

`.claude/templates/ux-checklist/product-ux-checklist-ja.md` §0 の 5 問に答える。
**OSS なので 4 問目は「作った人がいなくなっても続くか」に置き換わる。**

### 人はなぜこれを使いたいのか

**地図の見た目を自分で決めたいから。** そして日本語の地図は、見た目を決めるのが難しい。
地名の密度が高く、漢字・かな・英数が混ざり、鉄道は事業者ごとに色を持っている。
汎用のスタイルをそのまま当てると、日本の地図は**読めるが美しくない**か、
**美しいが読めない**かのどちらかになります。

### 人はなぜ今の方法をやめて、これを使うのか

今の方法には、やめる理由がそれぞれ別にあります。

| 今の方法 | やめる理由 |
|---|---|
| 商用地図 API | **キーと従量課金。**成功するほど費用が増える |
| OSM 公式タイル | **規約が商用・高トラフィックを認めていない。**動くことと、使ってよいことは別 |
| タイルサーバー自前運用 | **運用が増える。**地図を出したいだけの人が、サーバーを持つことになる |

### 自動化によって「良い面倒」まで消していないか

**消しかけています。ここが一番の分岐点です。**

スタイルは自動生成できます。配色をアルゴリズムで振れば、それらしいものは出ます。
しかし**地図の美しさは、人が色を選んだ結果としてしか出ません。**
スタイル JSON を生成物として扱った瞬間、この企画の唯一の差分が消えます。

→ **決定: スタイルは手書きの作品として扱う**（`.claude/decisions.md` D-002）。
ビルド生成物にしない。`.gitignore` で除外しない。レビュー対象にする。

### 作った人がいなくなっても、これは続くか

3 つとも満たす設計にします。

- [x] **手元に落として自分でビルドし、自分で公開できる。** タイル生成は公開データ
      （Protomaps 日次ビルド）と公開ツール（go-pmtiles）だけで完結し、
      **作者の環境にも鍵にも依存しません。**
- [x] **これが終了しても成果物は他所で読める。** PMTiles も MapLibre スタイルも
      このプロジェクトの独自形式ではなく、**既存の公開仕様**です。
- [x] **仕様と実装が分離している。** スタイルは MapLibre Style Spec v8、
      タイルは PMTiles 仕様。**実装が消えても様式は残ります。**

### AI が 10 倍賢くなっても、この製品は必要か

**必要です。** ここで必要なのは推論ではなく、**権利上使えるデータと、
人が選んだ配色**です。どちらも賢さでは代わりません。
むしろ AI が地図を読む機会が増えるほど、**帰属表示と再配布条件が明示された
ベース地図**の価値は上がります。

---

## 1. 作るもの

| # | 成果物 | 中身 |
|---|---|---|
| 1 | **ベースタイルの作り方** | Protomaps 日次**プラネット**ビルド → **必要な範囲を** `pmtiles extract` → 1 枚の PMTiles。**手元のファイルに依存しない**再現可能ビルド |
| 2 | **地図スタイル** | MapLibre Style Spec v8 の手書き JSON。ダークを 1 枚目に。**地域ごとに持つ**（D-018） |
| 3 | **UI 部品** | Marker / Popup / Cluster / 自前 POI / 経路。Web Components を軸に、React・Vue はラッパ |
| 4 | **配信のやり方** | 静的配信 ＋ HTTP Range。サーバーを立てない |
| 5 | **デモ** | 上を全部使った 1 枚。**動くものが無い OSS は読まれません** |

### 対象地域（D-018・2026-09-21）

**道具は世界、スタイルは地域ごと。**

実装に日本を縛っている箇所は **3 つだけ**で、どれもデータか既定値だった:
region の多角形 / `localIdeographFontFamily` / スタイルの `text-field`。
**機能コードに日本は入っていない。**

| いま | 次 |
|---|---|
| 日本（スタイルが一番進んでいる） | 台湾・上海・ベトナム・シンガポール |

**広げても差別化は捨てない。**原案の差別化は「地名の密度・漢字かな英数の混在・
事業者ごとの路線色」という**その地域固有の条件へ向けて作り込むこと**だった。
**どの地域のスタイルも中途半端になったら、広げるのをやめて 1 地域を仕上げる。**

## 2. やらないこと

- **タイル生成パイプラインの自作。** Protomaps の再実装に数か月を使って、
  差分がゼロになります。ここは借ります。
- **レンダリングエンジン。** MapLibre がやっています。
- **ジオコーダ・経路探索・地点データベース。** 別の製品です。
- **国土地理院の測量成果の利用。** 使わない限り測量法 29 条・30 条の承認申請は
  発生しません（`LICENSES.md`）。
- **費用削減を目的に掲げること。** 座標の取得元が「その提供者の地図の上に
  表示すること」を条件にしている場合、ベース地図だけを差し替える自由はありません。
  **効く条件が限られるものを、効果として約束しません**（D-008）。

## 3. 止めるべき条件

次のどれかが起きたら、**続ける前に企画を見直します**。

- Protomaps の日次プラネットビルドが止まる、または利用条件が変わる
- 手書きスタイルの差分が、汎用スタイルと見分けがつかない水準に留まる
  （＝この企画の唯一の差分が無い）
- UI 部品が、MapLibre の素の API を呼ぶのと変わらない厚さにしかならない

## 4. 技術の骨子

| 層 | 採るもの |
|---|---|
| レンダラ | MapLibre GL JS |
| タイル形式 | PMTiles |
| タイル生成 | Protomaps 日次プラネットビルド + go-pmtiles CLI（fork も再実装もしない） |
| 配信 | 静的配信で HTTP Range 206 を返せる経路 |
| スタイル | MapLibre Style Spec v8 の**手書き** JSON |
| UI 部品 | Web Components。React / Vue は薄いラッパ |
| パッケージ管理 | pnpm のみ |

### 実装で先に分かっていること

同じ構成を**本番稼働しているサイトで一度通してあり**、そこで踏んだ穴が分かっています。
S1 はこれを前提に進めます。

- **PMTiles を 1 枚置くだけの形にしない。** 配信側が `Range:` に 206 ではなく
  200（全量）を返す実装があり、そうなると 1 タイル見るたびに全体が落ちてきます。
  **配信経路が Range に正しく応えることを、疎通確認の項目にします。**
- **グリフは範囲をすべて用意する。** 1 つでも 404 になると、レンダラが地図全体を
  失敗として扱い、**画面が真っ白になります。**
- **オブジェクトストレージへの単発アップロードには上限がある。** 全国ぶんの
  PMTiles は超えるため、**分割アップロードできる経路を使います。**
