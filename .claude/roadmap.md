# ロードマップ

**原案 §25 の Phase をそのまま使いません。** 同じ構成を**本番稼働しているサイトで
一度通してあり**、原案が Phase 0 / 1 / 4 として置いていたものは、
**そこで既に答えが出ています**。

| 原案の Phase | 中身 | 現状 |
|---|---|---|
| Phase 0 | 権利・ライセンス調査 | **済。** 結論は `LICENSES.md` |
| Phase 1 | 一都市 PoC（道路・建物・水域・鉄道・地名・スタイル・PMTiles・表示） | **通過済。** 先行実装で稼働 |
| Phase 2 | 基本 UI（Marker / Popup / Cluster） | **先行実装の中に、そのサイト固有の形で存在。**切り出しが必要 |
| Phase 3 | フレームワーク連携（React / Vue） | 未着手 |
| Phase 4 | 日本全国 | **通過済。**全国 bbox で稼働 |
| Phase 5 | ドキュメントと公開 | このリポジトリ |

**したがって最初の仕事は新規実装ではありません。切り出しです。**

---

## S1 — 切り出して、先行実装の外で動かす

**このスプリントの完了条件は 1 つ。「先行実装を一切参照せずに、地図が 1 枚出ること」。**

- [x] pnpm workspace としてリポジトリの骨格を作る（2026-09-15）
- [x] ~~先行実装から取り出す~~ → **切り出し元が最後まで渡らなかったため、
      取り出しではなく書き起こしになりました**（2026-09-16）。
      結果として「先行実装を一切参照していない」ことは自動的に満たされています。
  - [x] 手書きダークスタイル（JSON）— `styles/modern-dark.json`（22 レイヤ・自分で書いた）
  - [x] タイル配信の経路（HTTP Range 206 を返す部分）
        — 手元 `tools/serve` ／ 本番 `infra/pmtiles-worker`（R2）。
        解釈は `packages/http-range` の 1 実装を共有。`pnpm tiles:check-range` で
        **206 を実測済み**（kansai / japan とも）
  - [x] グリフ取得 — **Protomaps 配布のフォントに CJK が無い**ため、
        `localIdeographFontFamily` で閲覧側のフォントに逃がして通しました。
        **これは決着ではありません**（decisions.md 未決・S3 で組版をどうするか決める）
- [x] **タイル生成ワークフローは書き直す。** 先行実装のローカル生成スクリプトには
      特定の PC のパス前提が残っており、**再現可能ビルドの条件を満たしません**。
      公開データと公開ツールだけで完結する形に作り直します
      → `tools/tiles` / `docs/tiles/README.md`
      （2026-09-16 に **kansai / japan とも実行済み**。`pmtiles verify` 通過）
- [x] `apps/demo/` で**素の地図が 1 枚**出ることを確認する（2026-09-16）
      — 実物は `docs/screenshots/`（大阪・梅田・関西・全国・東京・京都・那覇・札幌）
- [x] 帰属表示（`© OpenStreetMap contributors`）が画面に出ていることを確認する
      — 撮った 8 枚すべてに写っています

### S1 の状態（2026-09-17・**完了**）

**完了条件「先行実装を一切参照せずに、地図が 1 枚出ること」を満たし、
人が実物を見て判定しました**（ベースルール §29）。

- 2026-09-17、**人が 15 枚を見た。**
- **指摘は 1 件**。「ポップアップがださい・せまい・謎の四角がある」。
  → 閉じるボタンが短い文字に重なって潰れていた。直して撮り直した（`4b6a933`）。
- **他は指摘なし。**

AI 側が挙げていた懸念（z12 で「大阪市」のラベルが衝突で消えている／z15 の丁目密度／
z9 の緑の強さ／まとまりの丸の色）は、**人からは問題として挙がりませんでした。**
**現状で良いということなので、これらは直しません**（直すなら改めて指示が要る）。

**ここで S1 を閉じます。**

### 切り出さないもの

- **先行実装の地図コンポーネント本体。** そのサイトの業態別 UI と不可分に混ざっており、
  **単純コピーになりません。**必要な骨だけを S2 で書き直します。

---

## S2 — UI 部品を Web Components にする

- [x] Map / Marker / Popup を Web Components として書く（2026-09-16・`packages/elements`）
      — ビルド工程なし（素の ESM）。属性の解釈は純粋関数でテスト済み（14 件）。
      実物は `docs/screenshots/2026-09-16-elements-*.jpg`
- [x] Cluster — `<mmj-cluster>`（2026-09-17）。**まとめる計算は MapLibre の GeoJSON source**
      に任せ、部品は 3 枚のレイヤを排他の filter で置くだけ（D-011）。
      件数は色ではなく大きさで表す（配色は人の領域・§11）。
      実物は `docs/screenshots/2026-09-17-cluster-z11.jpg` / `-z15.jpg`
- [x] **利用者が自前の POI を持てるようにする**（`<mmj-poi>`・2026-09-17）
      — ベース地図には触らず、別 source で重ねる。名前を取る属性は `label-key` で
      指定できる（媒体ごとに違うため）。実物は `docs/screenshots/2026-09-17-poi-z15.jpg`。
      設計は `.claude/proposals/2026-09-17-media-poi.md`
- [x] **素の MapLibre を呼ぶのと変わらない厚さなら、そこで止める**（PRD §3）
      — **止めていません。**素の呼び出し 40 行に対し、部品を使うと 10 行。
      差の中身と「ここから厚くしない」約束は `docs/elements/README.md` に書きました

## S3 — スタイルを作品として仕上げる

- [ ] Modern Dark を正式なテーマ 1 枚目にする
- [ ] ライトテーマ
- [ ] **鉄道の路線色は、事業者が決めている色をそのまま写す。**
      独自配色に置き換えない（読む人が混乱します）
- [ ] 日本語のラベル配置（地名の密度・漢字かな英数の混在）
- [ ] **ベース地図の POI を描く** — 駅しか描いていないことが実測で分かった
      （`.claude/proposals/2026-09-17-poi.md`）。**データには入っている。**承認待ち。
      **S2 ではなくここに置く。**どの kind をどの倍率で出すかはスタイルの判断で、
      部品の話ではない（2026-09-17 に移した）

## S4 — 公開して、使えるようにする

- [ ] npm パッケージとして配る
- [ ] ベースタイルの入手手順（自分でビルドする道を必ず残す）
- [ ] React / Vue ラッパ
- [ ] ドキュメントサイト

---

## 進め方の約束

- **S1 が終わるまで、S2 以降に手を出さない。**切り出しが終わらないうちに
  新機能を書くと、切り出し元と新規実装が混ざって、どちらも終わらなくなります。
- **各スプリントの完了条件は「動くものが 1 つ出ること」。**
  ドキュメントが揃ったことを完了にしない。
